反向代理和正向代理的原理是一样的

不同的地方：1）正向代理和客户端在同一个局域网内。相当于每个客户端都有自己的一个正向代理软件
	    2）方向代理和服务端在同一个局域网内。相当于客户端只能看到反向代理，而看不到真正的服务器位置


方向代理，服务端把自己注册到反向代理中，让反向代理全权负责。
正向代理，服务端对外部完全透明，当时客户端想要访问的话，必须通过正向代理(有点白名单的意思)


小结：最主要的就是 客户端 代理 服务器 这三者，谁和谁是在同一个LAN(局域网)。

	客户端+代理  -->  正向代理
	代理+服务端  -->  反向代理

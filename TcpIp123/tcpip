#1 - accept每次只返回一个链接，其他的链接请求会被排队，知道在再调用accept

#2 - 应用层可以直接操作ip层，使用saw socket，ping就是使用raw socket实现的

#3 - 应用层+表示层+会话层：具体的应用协议层，应用的逻辑细节在这里体现，但是不关系通讯的细节
     传输层+网络层+数据链路层+物理层：不涉及应用，主要处理”发送数据“、”等待确认“ 、”给无序到达的数据排序“、”计算并验证校验和“等

     上三层一般为用户程序的组成部分，下四层则为内核的组成部分；这句话的意思也说明上三层是需要程序员自己实现的

